description: >
  A Docker-based Node executor, defaulting to cimg/node:lts
docker:
  - image: "cimg/node:<<parameters.tag>>"
parameters:
  tag:
    default: lts
    description: >
      Pick a specific circleci/node image variant:
      https://hub.docker.com/r/cimg/node/tags
    type: string
